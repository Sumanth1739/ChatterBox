actually pages contains the user interface and respective function
1)if in case of login page on handle submit we send the data(send the form data) to checksuthstore function login
2)if in case of signup page on handle submit we send the data(send the form data) to checksuthstore function login
3)
